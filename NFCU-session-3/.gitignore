# ABOUTME: Session-local ignore rules for NFCU Session 3 build artifacts.
# ABOUTME: Keeps the Python venv, caches, and Terraform state out of git.
.venv/
__pycache__/
*.pyc
.pytest_cache/
.terraform/
*.tfstate
*.tfstate.*
crash.log
# Lambda build zips
*.zip
# Reference parquet is regenerated by the capture script; keep the fixture instead
