# Runtime dependencies for drift-simulator (zip package).
#
# boto3 is provided by the Lambda Python 3.11 runtime.
# pandas, numpy, and pyarrow are provided by the AWS SDK for pandas managed
# Lambda layer (AWSSDKPandas-Python311). Versions below are for local test parity.
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
