# Python dependencies for the NFCU Session 1 training and pipeline scripts.
# Pinned to exact patch versions for reproducible builds (no ranges).
# Versions verified together against Python 3.11+; the SageMaker XGBoost 1.7-1
# serving container ships its own runtime and does not read this file.
xgboost==3.2.0
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.8.0
boto3==1.43.15
jsonschema==4.26.0
